/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost"], // Add any other image domains you'll use
  },
  // Enable static exports if needed
  // output: 'export',
};

module.exports = nextConfig;
